<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category</title>
    <link rel="stylesheet" href="/css/edit-category.css"> <!-- Assuming style.css contains the desired styles -->
</head>
<body>
    <div class="container">
        <h1>Edit Category</h1>
        <form action="/update-category/<%= category.category_id %>" method="POST">
            <div class="form-field">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= category.name %>" required>
            </div>
            <div class="form-field">
                <label for="description">Description:</label>
                <textarea id="description" name="description" required><%= category.description %></textarea>
            </div>
            <button type="submit">Update Category</button>
        </form>
    </div>
</body>
</html>
